# Results Manifest

The canonical results manifest is:

- `docs/08-results-manifest.md`

Final curated result JSON files are under:

- `results/final/` — 10 models × {ot-lite_gsma, ot-full_gsma} × 3 repeats
  = 60 `results_*.json` + 20 `_aggregate.json` (mean ± spread).

Score table and per-model analysis: `docs/04-final-results.md`.

Raw sample dumps, model weights, Hugging Face cache, and historical debug logs are
**not** part of this delivery package.
